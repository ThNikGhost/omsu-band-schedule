[package]
name = "omsu-sync-core"
version = "1.0.0"
edition = "2021"
description = "Settings and wire protocol for the OmSU schedule sync plugin"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
